#include<stdio.h>
#define MAX 20

int main()
{   
        int n,i,array[MAX];
	printf("enter:");
	scanf("%d",&n);
	
	for(i=0;i<n;i++)
	{
	printf("array[%d] = ",i);
	scanf("%d", &array[i]);
	}
	
	for(i=n-1;i>=0;i--)
	{
        printf("array[%d] = %d\n",i,array[i]); 
	}
}
